#include <cstdio>

int main() {
        // int score = 85;

        // score >= 60 ? printf("Aprobaste") : printf("Reprobaste");

        // -------------------------------------------------------------------------------------------------------------------------

        // int number = 10;

        // number % 2 ? printf("El nro %d es impar !", number) : printf("El nro %d es par !", number);

        // -------------------------------------------------------------------------------------------------------------------------

        bool hambriento = false;

        hambriento ? printf("Tienes mucha hambre...!") : printf("Estas full...!");

        return 0;
}